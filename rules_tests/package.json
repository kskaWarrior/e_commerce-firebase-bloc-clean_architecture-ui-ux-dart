{
  "name": "rules-tests",
  "private": true,
  "type": "module",
  "description": "Firestore security-rules tests. Run via: firebase emulators:exec --only firestore \"npm --prefix rules_tests test\"",
  "scripts": {
    "test": "mocha firestore.rules.test.mjs --timeout 30000"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.10.0",
    "mocha": "^10.7.3"
  }
}
